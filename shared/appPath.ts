/** Public URL prefix for the SPA (no trailing slash). */
export const APP_BASE_PATH = '/office'

/** Same as Vite `base` — always with a trailing slash. */
export const APP_BASE_URL = `${APP_BASE_PATH}/`
